services:
  web:
    command: gunicorn retirement_planner.wsgi:application --bind 0.0.0.0:8000 --workers 3 --timeout 120
    volumes: []  # No source mount in prod — use built image only
    ports:
      - "127.0.0.1:8002:8000"  # Nginx proxies in
