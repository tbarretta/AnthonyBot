services:
  web:
    command: gunicorn giftregistry.wsgi:application --bind 0.0.0.0:8000 --workers 2 --timeout 60
    ports:
      - "127.0.0.1:8001:8000"  # Only accessible from localhost — Nginx proxies in
  celery:
